# ============================================================
# requirements.txt — Python dependencies for the Data pipeline
# Pinned to stay compatible with apache-airflow==2.9.1
# DO NOT add apache-airflow here — it is already installed
# in the base image and must not be upgraded.
# ============================================================

# ----- Airflow providers (compatible with 2.9.1) -----
apache-airflow-providers-postgres==5.10.2
apache-airflow-providers-http==4.10.1

# ----- Data processing -----
pandas==2.1.4
numpy==1.26.4
pyarrow==14.0.2
openpyxl==3.1.2

# ----- Database -----
psycopg2-binary==2.9.9

# ----- HTTP / APIs -----
requests==2.31.0

# ----- Utilities -----
python-dotenv==1.0.1
tenacity==8.2.3

# ----- Testing -----
pytest==8.0.0
pytest-cov==5.0.0